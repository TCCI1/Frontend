import Image from 'next/image';

const RoadMap = () => (
  <section id="roadmap" className="py-20 bg-white flex flex-col items-center">
    <h2 className="text-3xl md:text-4xl font-bold mb-8 text-center text-black">
      Roadmap
    </h2>
  </section>
);
export default RoadMap;
